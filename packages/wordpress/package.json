{
  "name": "@everycal/wordpress",
  "version": "0.1.0",
  "private": true,
  "license": "AGPL-3.0-only",
  "description": "WordPress plugin with Gutenberg block for displaying EveryCal event feeds",
  "scripts": {
    "build": "wp-scripts build",
    "dev": "wp-scripts start",
    "lint": "wp-scripts lint-js",
    "plugin-zip": "wp-scripts plugin-zip",
    "i18n:compile": "for f in languages/*.po; do msgfmt -o \"${f%.po}.mo\" \"$f\"; done",
    "i18n:pot": "wp i18n make-pot . languages/everycal.pot --domain=everycal"
  },
  "devDependencies": {
    "@wordpress/scripts": "^31.0.0"
  }
}
